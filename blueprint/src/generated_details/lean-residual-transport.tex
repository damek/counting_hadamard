\medskip
\paragraph{Formal Lean statement.}
\begin{verbatim}
private lemma residual_transport_scaled
    (n t : ℕ) {c cTr CTr : ℝ}
    (hn : 3 ≤ n)
    (ht_tr : (t : ℝ) ≥ CTr * (n : ℝ) ^ (3 : Nat))
    (ht_pos : 0 < (t : ℝ))
    (hc_le_tr : c ≤ cTr)
    (htransportExp :
      ∀ n : ℕ, 3 ≤ n →
      ∀ t : ℕ, (t : ℝ) ≥ CTr * ↑n ^ (3 : Nat) →
        Real.exp (-(1 / 2 : ℝ) * (t : ℝ))
          ≤ Real.exp (-(cTr * (n : ℝ) ^ (2 : Nat))) * gaussianScale n (t : ℝ)) :
    |normalizedCount n (4 * t) - Cn3Torus.texPrefactor n * Cn3Torus.localIntegral n t|
      ≤ Real.exp (-(c * (n : ℝ) ^ (2 : Nat))) * gaussianScale n (t : ℝ)
\end{verbatim}
\begin{proof}[Proof sketch]
Once the threshold package is fixed, compare the local residual normalization with gaussianScale and transport bounds between them. The proof is a monotonicity and scaling argument rather than a new analytic estimate.
\end{proof}
\paragraph{Role in the route.}
It is used directly by residual\_estimate\_quantitative.
\paragraph{Source location.}
\texttt{RequestProject/HadamardCn3LocalGapResidual.lean:85}
\medskip
